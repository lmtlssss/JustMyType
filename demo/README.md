# file-effect proof

The fixed-command comparison uses two disposable copies of 10,000 synthetic
invoice records. It records actual API decisions, file effects and checksums.
It is not an autonomous-agent baseline.

```sh
python demo/prove.py --live --output proof-local.json
```

Supply the API key through the environment. See [recorded results](proof.json).
