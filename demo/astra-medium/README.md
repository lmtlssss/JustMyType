# Astra / medium

[Watch the 40-second recording](JustMyType-Astra-Medium.mp4).

Same model. Same request. Same starting files.
Left: without JustMyType. Right: with JustMyType.

Both fixed the checkout, passed 4,096 checks, deployed version 2.0,
and preserved all 10,000 test records. Both generated working checkout pages.
The plugin run also checked actions and the final claim through TypeSafe.
This pair does not establish a correctness or speed advantage.

The video uses real screen captures. Cuts and speed changes are marked.
Both sides use the same timing. No output is reconstructed.
A private path appeared in a diagnostic step; that interval is removed
from both sides. The original capture is retained privately.

The browser section shows the actual generated pages and live input changes.
All twelve total checks passed. No real payment or customer data is involved.

[Prompt](prompt.txt) / [Results and source hashes](proof.json)

The unmodified pages are included as [without.html](without.html) and
[with.html](with.html). Open either file locally. They need no dependencies.
